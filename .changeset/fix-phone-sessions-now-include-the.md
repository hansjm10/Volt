---
"@hansjm10/volt-coding-agent": patch
---

fix(remote): Phone sessions now include the inspect and lsp tools by default, so plan mode on a paired device can use vetted Git/GitHub inspection and language-server reads like the TUI. ([#153](https://github.com/volt-hq/Volt/issues/153))
