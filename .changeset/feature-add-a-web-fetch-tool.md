---
"@hansjm10/volt-coding-agent": patch
---

feature(web-fetch): Add a web_fetch tool that reads a single URL as text, with SSRF protection and output size limits, so agents can follow up on web_search results without pulling whole pages into context.
