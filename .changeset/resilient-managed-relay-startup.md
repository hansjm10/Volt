---
"@hansjm10/volt-coding-agent": patch
---

fix(daemon): Managed relay endpoints now stay available during stalled networking without reusing expired or revoked credentials across startup and shutdown.
